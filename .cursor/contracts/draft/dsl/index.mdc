# draft/dsl/ Index

DSL & YAML contracts: circuit syntax, data file cleanup, and playbook documentation.

## Execution order

```
dsl-lexicon              ✓ COMPLETE (completed/dsl-migration/)
dsl-wiring               ✓ COMPLETE (completed/dsl-migration/)
  ├──▶ circuit-dsl-shorthand          ← UNBLOCKED (not started)
  ├──▶ yaml-dx-cleanup                ← IN PROGRESS (P0,P1,P6 done; P2 partial; P3-P5 remain)
  ├──▶ yaml-playbook-docs             ← UNBLOCKED (not started, benefits from dx-cleanup)
  └──▶ circuit-subgraph-composition   ← IN PROGRESS (P1-P3 core done; fold validation + lint remain)
```

- **circuit-dsl-shorthand** — unblocked. Not started.
- **yaml-dx-cleanup** — Phases 0 (housekeeping), 1 (domain dirs), 6 (manifest clarity) complete. Phase 2 (naming) partially done. Phases 3 (scenario defaults), 4 (payload externalization), 5 (enforcement) not started.
- **yaml-playbook-docs** — no code dependency; can run any time. Benefits from yaml-dx-cleanup landing first (fewer files to document).
- **circuit-subgraph-composition** — P1-P3 core implementation done (framework `handler_type: circuit`, Knowledge transformers + circuit YAML, RCA `gather-code` node). Remaining: P1.6 fold validation, P1.7 lint rule, P3.4 hook deprecation, P3.5 autodoc rendering.

## Files
- `circuit-dsl-shorthand.md` — Cohesive node-scoped edges, auto-inferred topology flags, implicit defaults for circuit YAMLs. **Quick-win.**
- `yaml-dx-cleanup.md` — Self-documenting names, zero redundant identity, vocabulary shorthand, schema collapse, scenario defaults, payload externalization. **Quick-win (remaining phases).**
- `yaml-playbook-docs.md` — Extend autodoc-generated skeleton with Asterisk narrative, Ansible analogy, project identity. **Quick-win.**
- `circuit-subgraph-composition.md` — Composable circuits as subgraph nodes via `handler_type: circuit`. Knowledge/RCA/calibration are all peers. **Quick-win (tail items).**
