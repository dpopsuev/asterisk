# .cursor/rules/domain Index

Asterisk project rules — committed, shared; specific to this repository and domain.

## Files
- `project-standards.mdc` — Product definition, methodology (ROGYB, Gherkin), calibration ordering, data handling, output.
- `agent-operations.mdc` — Local circuit stages, timeout SLAs, abort protocol, observability.
- `knowledge-store.mdc` — .cursor/ structure, index integrity, persistence protocol, work contracts.
- `contract-iteration.mdc` — Monitor contracts for insights from discussions and calibration runs.
- `architecture-snapshots.mdc` — Architecture diagrams as first-class artifacts; current/desired state snapshots.
- `data-hygiene.mdc` — Prevent re-introduction of scrubbed internal hostnames; placeholder values.
- `workspace-context.mdc` — Local repos as source of truth; version alignment (RP 5.11.x).
- `human-readable-output.mdc` — Codes for machines, words for humans; internal/display registry.
- `contract-sizing.mdc` — Contract sizing and urgency classification; goal alignment check.
