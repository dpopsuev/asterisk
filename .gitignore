# Report Portal API key (never commit)
.rp-api-key

# Private / development-only data (never commit)
# Contents of .dev/ are ignored; .dev/README.md is kept so the folder's purpose is documented
.dev/*
!.dev/README.md

# Binaries / build output
/bin/
/dist/
/asterisk
/asterisk-bin
/mock-calibration-agent
*.exe
*.exe~
*.dll
*.so
*.dylib

# Cursor local MCP config (contains machine-specific paths)
.cursor/mcp.json

# Personal developer preferences (never commit — per-developer)
.cursor/personal/

# Sensitive infrastructure details (never commit — security compartment)
.cursor/sensitive/

# Local runtime directory
.asterisk/

# Meta-calibration run data (empirical, append-only, not committed)
pkg/framework/metacal/runs/*.json
# Local metacal binary (go build output, repo root only)
/metacal

# Go
*.test
*.out
go.work

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
*~

# Local DB (per-workspace or dev)
*.db
*.db-journal

# Jira exports (private data, never commit)
.jira/

# Env and secrets
.env
.env.local
*.pem
