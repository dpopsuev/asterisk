kind: artifact-schema
version: v1
metadata:
  name: F0_RECALL
  description: "Recall step artifact schema"

name: F0_RECALL
fields:
  match:
    type: bool
    required: true
  confidence:
    type: float
    required: true
  reasoning:
    type: string
    required: true
