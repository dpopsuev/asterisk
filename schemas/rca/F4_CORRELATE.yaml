kind: artifact-schema
version: v1
metadata:
  name: F4_CORRELATE
  description: "Correlate step artifact schema"

name: F4_CORRELATE
fields:
  is_duplicate:
    type: bool
    required: true
  confidence:
    type: float
    required: true
